package com.example.demo.professor_materia.repository;

import com.example.demo.professor_materia.model.ProfessorMateria;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ProfessorMateriaRepository extends JpaRepository<ProfessorMateria, Long> {
}
